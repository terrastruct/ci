name: ci
on: [push]
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  assert_linear:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: COLOR=1 ./assert_linear.sh
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: COLOR=1 ./ci/test.sh
  gen_lib:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: COLOR=1 ./ci/gen_lib.sh
